create table customer (
    customer_id varchar PRIMARY KEY,
    name varchar,
    phone_no text,
    address text,
    cart_id varchar,
    password varchar,
    email_id varchar 
)
select * from customer
drop table customer
