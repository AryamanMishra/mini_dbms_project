<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <% if(category_name[category_name.length - 1] === 's' ) { %> 
            <%= category_name.substring(0,category_name.length - 1) %> Products 
        <% } else { %>
            <%= category_name %> Products 
        <% } %> 
    </title>
</head>
<body>
    <h1>
        <% if(category_name[category_name.length - 1] === 's' ) { %> 
            <%= category_name.substring(0,category_name.length - 1) %> Products 
        <% } else { %>
            <%= category_name %> Products 
        <% } %> 
    </h1>
    <ol>
        <% for (let p of products) {%>
            <form action="/cart" method="post">
                <input type="text" hidden name="product_id" value="<%= p.product_id %>">
                <li>Name: <%=p.product_name %>
                <br>
                Company: <%= p.brand %>
                <br>
                Price: &#8377;<%= p.price %>
                <br>
                Discount: &#8377;<%= p.discount %>
                <br>
                Is It available: 
                    <% if (p.availability === true) { %>
                        Yes 
                    <% } else { %>
                        No   
                    <% } %> 
                <br>
                </li>   
                <br>
                <button type="submit">Add to Cart</button>
            </form>
            <br>
        <% } %>  
    </ol>
    <button><a href="/users/<%= customer_id %>" style="text-decoration: none;">Back to User Page</a></button>
</body>
</html>